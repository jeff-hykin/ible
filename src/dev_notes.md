- the "source of truth" validation of observation is  `validateObservation()` in "utils.js"
- `window.player` is a hack, mostly because plyr (library) is broken for local videos (but works* for youtube videos)